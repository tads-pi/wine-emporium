import axios from "axios"
import React, { useState } from "react"
import { Controller } from "react-hook-form"

type enderecoProps = {
    campo: any
    control: any
    index: any
    register: any
}


export default function Endereco({ campo, control, index, register }: enderecoProps) {
  
}