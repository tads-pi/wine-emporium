import React from "react"

interface CheckoutResumeProps {
    handleNext: () => void,
    goHome: () => void,
}

export default function CheckoutResume(props: CheckoutResumeProps) {
    return (
        <div>
            CheckoutResume
        </div>
    )
};
