name: PRE 🍷 Backoffice Deploy

on:
  pull_request:
    branches: [main, dev]
    paths:
      - 'backoffice/**'
      - '.github/workflows/deploy-backoffice.yml'

jobs:
  test:
    name: 🍷 Lint & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
        with:
          node-version: 18
      - run: npm ci
      - run: npm run lint
